#include <gtest/gtest.h>

/* TODO: for all, port over PANDORA knowledge harness */

TEST(SquirrelKnowledgeBase, basicTest){
  EXPECT_TRUE(true);
}

TEST(SquirrelKnowledgeBase, knowledgeAddition){
  EXPECT_TRUE(true);
}

TEST(SquirrelKnowledgeBase, knowledgeRemoval){
  EXPECT_TRUE(true);
}

TEST(SquirrelKnowledgeBase, filterTest){
  EXPECT_TRUE(true);
}

int main(int argc, char** argv){
  testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
