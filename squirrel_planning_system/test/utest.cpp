#include <gtest/gtest.h>

TEST(SquirrelPlanningSystem, knowledgeTest) {
	// TODO port PANDORA knowledge base harness	
	EXPECT_TRUE(true);
}

TEST(SquirrelPlanningSystem, actionDispatchTest) {
	// TODO port PANDORA domain/action dispatch	
	EXPECT_TRUE(true);
}

int main(int argc, char** argv){
  testing::InitGoogleTest(&argc, argv);
  return RUN_ALL_TESTS();
}
